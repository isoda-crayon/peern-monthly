# 月刊ぴあん — 作業のときの決まり

保護者の方が読む公開ページです。GitHub Pages で配信していて、**認証はありません**
（https://isoda-crayon.github.io/peern-monthly/ ）。
リポジトリも public なので、ここに置いたものは全部そのまま外から見えます。

## 外に出してはいけないもの

- 原本PDFの保管場所（共有ドライブのパス）、変換スクリプトの使い方、事業所の内部手順
  → 職員向けの手引きは README.md に集めてあります。**公開ページ側には書かない**
- 業務システムの画面キャプチャ。一部でも載せない
- 利用者名・職員名・学校名・住所・電話・メール・顔写真

`issues.js` に載るのは、課題名・週・ページ画像だけです。この形を崩さないこと。

## 開発日誌（`diary.html` = この ページの あゆみ）

あとりえ送迎システム（`C:\repos\sougei-system`）の `/dev-diary` を移植したものです。
向こうは Cloudflare Access の内側にありますが、**こちらは誰でも読めます**。
読み手も違います（向こう＝職員、こちら＝保護者）。記事を書くときはそこを取り違えないこと。

- `assets/diary.css` の**上から955行は移植元そのまま**です。直す前に
  「移植元も同じように直すか」を考える。それより下だけが月刊ぴあんぶんの追記
- カテゴリ（`しくみ` / `つかいかた` / `軌跡`）と `callout` の `tone`
  （`info` / `warn` / `tip` / `story` / `miss`）は **CSS 側に日本語・英語のまま書いてあります**。
  勝手に増やすとマステや吹き出しが無色になります。増やすなら CSS も同時に直す
- 記事を足したら `diary.js` の `latest` を上げる。上げないと入口の赤い点が誰にも点きません
- 日誌に載せてよい写真は、**このサイト自身の画面**だけです

## 画面の写真を撮るとき

写真は全部同じ幅で撮ります（`assets/diary.js` の `CAPTURE_W` / `CAPTURE_H`）。
寸法が混ざると `crop` の縦横比がずれます。撮り直すときも同じ幅で。
